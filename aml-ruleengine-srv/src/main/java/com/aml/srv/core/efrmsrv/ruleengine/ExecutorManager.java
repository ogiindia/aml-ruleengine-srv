package com.aml.srv.core.efrmsrv.ruleengine;

import org.springframework.stereotype.Component;

@Component
public class ExecutorManager {
	/*
	 * private final ThreadPoolTaskExecutor executor;
	 * 
	 * public ExecutorManager(ThreadPoolTaskExecutor executor) { this.executor =
	 * executor; }
	 * 
	 * @PreDestroy public void shutdownExecutor() {
	 * System.out.println("Shutting down async executor..."); executor.shutdown();
	 * // Gracefully stop accepting new tasks }
	 */

}
